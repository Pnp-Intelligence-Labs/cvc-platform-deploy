# Core API
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-multipart==0.0.26

# Database
psycopg2-binary==2.9.9

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
pyotp>=2.9.0

# HTTP
requests==2.31.0

# Config
python-dotenv==1.0.0

# Document processing (partner doc ingestion)
pdfplumber==0.11.9
python-docx==1.1.2

# Object storage
minio==7.2.20

# ML / recommendations
numpy>=1.26.0
sentence-transformers>=3.0.0   # PnPbert semantic encoder (all-MiniLM-L6-v2); falls back to TF-IDF if absent

# Google Drive integration
google-auth>=2.40.0
google-auth-oauthlib>=1.4.0
google-auth-httplib2>=0.4.0
google-api-python-client>=2.179.0

# DD plugin — file conversion
markitdown>=0.1.0
pandas>=2.0.0
openpyxl>=3.1.5
python-pptx>=1.0.0   # markitdown needs this to extract PowerPoint pitch decks
