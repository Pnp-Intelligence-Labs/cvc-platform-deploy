__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.env
.venv
venv/
ENV/
*.log
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Credentials — never commit
config/team_credentials.json
client_secret.json
**/client_secret.json

# DD pipeline — dataroom files and pipeline outputs are confidential
workers/dd/workdir/
plugins/_staging/workers/dd/workdir/

# Generated knowledge graph — large, rebuild with `graphify update .`
graphify-out/

# Claude Code / agent tooling — local only
.agents/
.claude/skills/
memory/
.enrichment_progress.json

# Installed plugins are tracked so managed deploys (Railway) ship with features enabled

# React frontend source — track src, ignore build artifacts
# designs/figma-export/ is Figma-only; figma-dashboard/ is production source
designs/figma-export/
designs/figma-dashboard/node_modules/
designs/figma-dashboard/dist/

# Playwright
tests/e2e/.auth/user.json
tests/e2e/report/
test-results/
node_modules/
.vercel

# Test data archives — large, rebuild from source
cvc_test_data/
cvc_test_data.zip

# Sensitive documents — partner contracts and confidential dataroom files
workers/rm/

# Drive ingestion staging cache (text persisted to DB)
/workdir/
